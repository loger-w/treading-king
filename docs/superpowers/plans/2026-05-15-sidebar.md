# Sidebar + Multi-page Shell — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 frontend 從單頁(只有 Monitor)變成左側可收合 sidebar 的 multi-page shell,放兩個 nav 項:即時監控、小台指策略回測 (MXF)(後者僅 placeholder)。

**Architecture:** `App.tsx` 用 `useState<'monitor' | 'backtest'>` 切頁,不裝 react-router。Monitor 與 backtest placeholder **always-mount + CSS `hidden`** 切換,避免 Monitor 切走時斷 WS 重連。Sidebar 展開/收合狀態以 localStorage 持久化。沿用既有 Editorial Dark 色票與字體,不引入新色或新依賴。

**Tech Stack:** React 18、Vite、TypeScript、Tailwind(既有 `tailwind.config.js`)。**不寫 Vitest** — UI shell 採手動瀏覽器驗收(spec 已敲定)。Dev server 已在 http://localhost:5173 跑,每 task 完成後在那邊驗。

**Spec:** `docs/superpowers/specs/2026-05-15-sidebar-design.md`

---

## File Structure

| File | Action | Responsibility |
|---|---|---|
| `frontend/src/hooks/useSidebarState.ts` | Create | localStorage 持久化 expanded boolean |
| `frontend/src/components/Sidebar.tsx` | Create | 漢堡按鈕 + nav items + 收合動畫 |
| `frontend/src/pages/MXFBacktest.tsx` | Create | placeholder 頁(後續實作回測引擎) |
| `frontend/src/App.tsx` | Modify | 包 sidebar + 頁面切換 state + always-mount |

---

## Task 1: `useSidebarState` hook

**Files:**
- Create: `frontend/src/hooks/useSidebarState.ts`

- [ ] **Step 1: Create hook file**

Write `frontend/src/hooks/useSidebarState.ts`:

```ts
import { useEffect, useState } from 'react';

const KEY = 'sidebar:expanded';

export function useSidebarState() {
  const [expanded, setExpanded] = useState<boolean>(() => {
    const raw = localStorage.getItem(KEY);
    return raw === null ? true : raw === '1';
  });

  useEffect(() => {
    localStorage.setItem(KEY, expanded ? '1' : '0');
  }, [expanded]);

  return [expanded, setExpanded] as const;
}
```

- [ ] **Step 2: Type-check**

Run: `cd frontend; npx tsc -b`
Expected: no errors

- [ ] **Step 3: Commit**

```bash
git add frontend/src/hooks/useSidebarState.ts
git commit -m "feat(frontend): add useSidebarState hook"
```

---

## Task 2: `Sidebar` component

**Files:**
- Create: `frontend/src/components/Sidebar.tsx`

- [ ] **Step 1: Create the component**

Write `frontend/src/components/Sidebar.tsx`:

```tsx
import { useSidebarState } from '../hooks/useSidebarState';

export type Page = 'monitor' | 'backtest';

interface NavItem {
  id: Page;
  label: string;
  iconPath: string;
}

const NAV_ITEMS: NavItem[] = [
  {
    id: 'monitor',
    label: '即時監控',
    iconPath: 'M3 12h3l3-8 4 16 3-10 2 6h3',
  },
  {
    id: 'backtest',
    label: '小台指策略回測 (MXF)',
    iconPath: 'M3 3v18h18 M7 14l4-4 3 3 5-7',
  },
];

interface Props {
  current: Page;
  onNavigate: (page: Page) => void;
}

export function Sidebar({ current, onNavigate }: Props) {
  const [expanded, setExpanded] = useSidebarState();

  return (
    <aside
      className={[
        'h-screen shrink-0 bg-bg-deep border-r border-line',
        'flex flex-col transition-[width] duration-200 ease-out',
        expanded ? 'w-[220px]' : 'w-[56px]',
      ].join(' ')}
    >
      <div className={expanded ? 'p-[18px] border-b border-line' : 'py-[18px] flex justify-center border-b border-line'}>
        <button
          type="button"
          onClick={() => setExpanded((v) => !v)}
          aria-label="切換選單"
          aria-expanded={expanded}
          className="flex flex-col gap-[3px] w-4 text-ink-muted hover:text-ink transition-colors duration-150 ease-out cursor-pointer"
        >
          <span className="block h-[1.5px] bg-current" />
          <span className="block h-[1.5px] bg-current" />
          <span className="block h-[1.5px] bg-current" />
        </button>
      </div>

      <nav className="flex flex-col py-2">
        {NAV_ITEMS.map((item) => {
          const isActive = item.id === current;
          return (
            <button
              key={item.id}
              type="button"
              onClick={() => onNavigate(item.id)}
              title={item.label}
              aria-current={isActive ? 'page' : undefined}
              className={[
                'relative flex items-center gap-3 transition-colors duration-150 ease-out cursor-pointer',
                expanded ? 'px-4 py-2.5' : 'px-0 py-3 justify-center',
                isActive
                  ? 'text-ink bg-accent/[0.05] before:absolute before:left-0 before:top-0 before:bottom-0 before:w-[2px] before:bg-accent'
                  : 'text-ink-muted hover:text-ink hover:bg-white/[0.02]',
              ].join(' ')}
            >
              <svg
                className={[
                  'w-4 h-4 shrink-0 transition-colors duration-150 ease-out',
                  isActive ? 'text-accent' : 'text-ink-dim',
                ].join(' ')}
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                strokeWidth="1.5"
                strokeLinecap="round"
                strokeLinejoin="round"
              >
                <path d={item.iconPath} />
              </svg>
              <span className={expanded ? 'text-sm font-medium' : 'sr-only'}>
                {item.label}
              </span>
            </button>
          );
        })}
      </nav>
    </aside>
  );
}
```

- [ ] **Step 2: Type-check**

Run: `cd frontend; npx tsc -b`
Expected: no errors (Sidebar is exported but not yet used — TS won't flag that)

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/Sidebar.tsx
git commit -m "feat(frontend): add Sidebar component"
```

---

## Task 3: `MXFBacktest` placeholder page

**Files:**
- Create: `frontend/src/pages/MXFBacktest.tsx`

- [ ] **Step 1: Create the page**

Write `frontend/src/pages/MXFBacktest.tsx`:

```tsx
export function MXFBacktest() {
  return (
    <div className="h-full flex flex-col items-center justify-center px-8">
      <span className="label-tiny mb-3">Module · Upcoming</span>
      <h1 className="h-display text-4xl text-ink mb-4">小台指策略回測 (MXF)</h1>
      <p className="text-ink-muted max-w-md text-center leading-relaxed">
        策略回測引擎開發中。
        <br />
        日內 1 分 / 5 分 K、bid/ask 推方向、規則編輯器、績效報表 — 後續實作。
      </p>
      <div className="mt-12 label-small">待規劃</div>
    </div>
  );
}
```

- [ ] **Step 2: Type-check**

Run: `cd frontend; npx tsc -b`
Expected: no errors

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/MXFBacktest.tsx
git commit -m "feat(frontend): add MXFBacktest placeholder page"
```

---

## Task 4: Wire Sidebar into `App.tsx`

**Files:**
- Modify: `frontend/src/App.tsx`

- [ ] **Step 1: Replace App.tsx content**

Current `frontend/src/App.tsx` 是 10 行,只 render `<Monitor />`。整個替換成:

```tsx
import { useEffect, useState } from 'react';
import { Sidebar, type Page } from './components/Sidebar';
import { Monitor } from './pages/Monitor';
import { MXFBacktest } from './pages/MXFBacktest';

export default function App() {
  const [page, setPage] = useState<Page>('monitor');

  useEffect(() => {
    document.body.classList.add('opacity-100');
  }, []);

  return (
    <div className="h-screen flex">
      <Sidebar current={page} onNavigate={setPage} />
      <div className="flex-1 min-w-0 overflow-hidden relative">
        <div hidden={page !== 'monitor'} className="h-full">
          <Monitor />
        </div>
        <div hidden={page !== 'backtest'} className="h-full">
          <MXFBacktest />
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Type-check**

Run: `cd frontend; npx tsc -b`
Expected: no errors

- [ ] **Step 3: Manual browser verification**

Dev server 已在跑(http://localhost:5173)— Vite HMR 會自動 reload。在瀏覽器測:

1. ✅ Sidebar 預設展開(220px),Monitor 在右邊正常顯示
2. ✅ 點漢堡 → 寬度滑到 56px,Monitor 寬度自動長出,過渡平滑(~200ms)
3. ✅ Reload 頁面(F5)→ 收合狀態保留
4. ✅ 點漢堡再展開 → 寬度滑回 220px,label 文字出現
5. ✅ 點「小台指策略回測 (MXF)」item → 切到 placeholder 頁面,active 指示移到第二項(2px accent 左邊條)
6. ✅ 切回「即時監控」→ Monitor 立即顯示,**檢查 WS 狀態指示燈是否還是「連線中」(綠)**,watchlist 內容、active signals 還在
7. ✅ 收合到 56px 時 hover icon → 出現原生 tooltip 顯示完整 label
8. ✅ Tab 鍵能依序走到漢堡 → monitor item → backtest item,Enter 觸發
9. ⚠️ **檢查 IntradayChart 重 fit**:切到 backtest 然後切回,圖表大小有沒有錯亂?若有錯亂(空白、縮成 0×0),記下來,Task 5 處理;若正常就跳過 Task 5。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/App.tsx
git commit -m "feat(frontend): wire Sidebar + multi-page shell into App"
```

---

## Task 5: (Conditional) Fix `IntradayChart` resize after hidden toggle

**Skip this task if Task 4 Step 3 #9 verified the chart renders correctly after toggle.**

**Files:**
- Modify: `frontend/src/components/IntradayChart.tsx`(具體行數待確認)

- [ ] **Step 1: Inspect IntradayChart resize logic**

Run: `cd frontend; grep -n "ResizeObserver\|resize\|fitContent" src/components/IntradayChart.tsx`
Expected: 找到 chart 既有的 resize 邏輯,通常會用 `ResizeObserver` 或 `chart.resize()` / `chart.timeScale().fitContent()`。

- [ ] **Step 2: Add IntersectionObserver to re-fit on become-visible**

把 IntradayChart 內部既有 ResizeObserver 那塊抓出來,旁邊加一個 IntersectionObserver:

```tsx
useEffect(() => {
  if (!containerRef.current || !chartRef.current) return;
  const el = containerRef.current;
  const io = new IntersectionObserver((entries) => {
    for (const e of entries) {
      if (e.isIntersecting && chartRef.current) {
        chartRef.current.resize(el.clientWidth, el.clientHeight);
        chartRef.current.timeScale().fitContent();
      }
    }
  });
  io.observe(el);
  return () => io.disconnect();
}, []);
```

(`containerRef` / `chartRef` 名稱以實際既有 code 為準,讀完 IntradayChart 再對齊。)

- [ ] **Step 3: Type-check**

Run: `cd frontend; npx tsc -b`
Expected: no errors

- [ ] **Step 4: Manual verify**

回 http://localhost:5173 重做 Task 4 Step 3 的 #9:切到 backtest → 切回 monitor → chart 是否正確 fit?
Expected: chart 顯示與初始載入一致,沒有 0×0 或空白。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/IntradayChart.tsx
git commit -m "fix(frontend): re-fit IntradayChart on become-visible after page toggle"
```

---

## Self-review checklist(寫完 plan 看一遍)

- ✅ Spec 涵蓋:`useSidebarState` (Task 1)、`Sidebar` 元件含全部視覺與互動 (Task 2)、`MXFBacktest` 內容 (Task 3)、`App.tsx` layout + always-mount (Task 4)、Chart resize 風險 (Task 5)
- ✅ 全部色彩 token 一致:`bg-bg-deep`、`border-line`、`text-ink-muted`、`text-ink-dim`、`text-accent`、`bg-accent/[0.05]`、`bg-white/[0.02]` — 都是既有 tailwind.config.js 定義的
- ✅ Type 一致:`Page` 在 `Sidebar.tsx` 定義並 export,`App.tsx` import `type Page`
- ✅ Icon SVG path 與 spec 一致
- ✅ 每 step 都有具體內容(code / 命令 / 預期),無 TBD / "implement later"
- ✅ Commit message 格式跟既有 commit 對齊(`feat(frontend):` / `fix(frontend):`)
